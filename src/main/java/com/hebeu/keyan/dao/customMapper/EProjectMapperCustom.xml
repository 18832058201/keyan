<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hebeu.keyan.dao.customMapper.EProjectMapperCustom">

    <!--查询指定id的项目详情-->
    <select id="selectProjectInfoById" resultType="com.hebeu.keyan.po.customPO.EProjectCustom">
        SELECT * FROM view_project p WHERE p.project_id = #{projectId}
    </select>

    <!--查询指定人员的项目集合-->
    <select id="selectProjectByWorkNum" resultType="com.hebeu.keyan.po.customPO.EProjectCustom">
        SELECT v.*,r.*,c.college_name FROM view_project v LEFT JOIN r_person_project_result r ON v.project_id = r.id LEFT JOIN e_college c ON r.college_id = c.college_id WHERE work_num = #{workNum}
    </select>
    <!--查询指定项目对应的人员列表-->
    <select id="selectPersonListByProjectId" resultType="com.hebeu.keyan.vo.PersonListVO">
        SELECT r.* FROM r_person_project_result  r
        WHERE r.id = #{projectId}
    </select>

</mapper>